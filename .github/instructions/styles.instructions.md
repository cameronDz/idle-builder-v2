---
applyTo: "src/**/*.module.css,src/index.css"
---

- Keep styles scoped to CSS Modules for component-specific styling.
- Prefer extending existing class patterns over introducing one-off naming schemes.
- Avoid broad global style changes unless requested.
- Preserve responsive behavior and avoid layout regressions.
